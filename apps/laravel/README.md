# Laravel App

Laravel benchmark target for the pilot matrix.

Routes:

1. `/healthz`
2. `/hello`
3. `/json`
4. `/db-read`
5. `/db-read-cache-warm`

The route semantics intentionally mirror `apps/native`.
